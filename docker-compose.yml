services:
  backend:
    build: ./backend
    env_file: .env
    environment:
      - PORT=5000
    expose:
      - "5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
